interface:
  display_name: "Official Question Import"
  short_description: "初始化、对话写题、预校验、预览并安全提交官方题批次，提交前自动走浏览器登录授权"
  default_prompt: "Use $official-question-import to initialize a workspace, turn conversation into official-question batches, preview them safely, and only submit after confirmation. When submit needs ops-admin access, start the built-in browser authorization flow instead of asking for account passwords."

policy:
  allow_implicit_invocation: true
